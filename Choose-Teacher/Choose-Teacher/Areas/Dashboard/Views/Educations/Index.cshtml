@model IEnumerable<Choose_Teacher.Models.Education>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Dashboard/Views/Shared/_DashboardLayout.cshtml";
}

<h1>Educations</h1>
@if (Context.Session.GetString("adminName") == null)
{
    <p>
        <a asp-action="Create">Add Education</a>
    </p>
}
<table class="table table-light">
    <thead>
        <tr>
            @if (Context.Session.GetString("adminName") != null)
            {
                <th>
                    @Html.DisplayNameFor(model => model.Teacher.TeacherName)
                </th>
            }
            <th>
                @Html.DisplayNameFor(model => model.Majoring)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.University)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GraduationYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @if (Context.Session.GetString("adminName") != null)
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
                    </td>
                }
                <td>
                    @Html.DisplayFor(modelItem => item.Majoring)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.University)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GraduationYear)
                </td>
                @if (Context.Session.GetString("adminName") == null)
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.EducationId"><i class="fas fa-pencil-alt"></i></a> |
                        <a asp-action="Details" asp-route-id="@item.EducationId"><i class="fas fa-eye"></i></a> |
                        <a asp-action="Delete" asp-route-id="@item.EducationId"><i class="fas fa-trash-alt"></i></a>
                    </td>
                }
                <td>
                    <a asp-action="Details" asp-route-id="@item.EducationId"><i class="fas fa-eye"></i></a> |

                </td>
            </tr>
        }
    </tbody>
</table>
